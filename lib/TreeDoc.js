var Node = function() {
  // major node may have several children nodes
};

var TreeDoc = function() {
  this._ident = ident;
  this._counter = 0;

  this._recalculateValue();
};

TreeDoc.prototype.insert = function (posID, atom) {

};

TreeDoc.prototype.delete = function (posID) {

};

return TreeDoc;
